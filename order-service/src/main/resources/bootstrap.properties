# Application name (must match the filename in config-repo: order-service.properties)
spring.application.name=order-service
server.port=8083

# Tell Spring Boot to fetch config from Config Server
spring.config.import=optional:config-server:http://localhost:8888

# Profile
spring.profiles.active=default